<div ng-controller="HomeDetailCtrl">
<center>
    <h1>Monitfy</h1>
    <p>Monitor Your Assets</p>
</center>

<!--div ng-repeat="sensor in sensors">
    <h2>{{sensor.name}}</h2><p>{{sensor.created_at}}</p>
    <p>{{sensor.description}}</p>
</div--!>
</div>
